import { BlackboardMessagePipe } from './blackboard-message.pipe';

describe('BlackboardMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new BlackboardMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
