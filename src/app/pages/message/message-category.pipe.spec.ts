import { MessageCategoryPipe } from './message-category.pipe';

describe('MessageCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
