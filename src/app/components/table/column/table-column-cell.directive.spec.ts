import {TableColumnCellDirective} from "./table-column-cell.directive";

describe('TableColumnCellDirective', () => {
  it('should create an instance', () => {
    const directive = new TableColumnCellDirective();
    expect(directive).toBeTruthy();
  });
});
